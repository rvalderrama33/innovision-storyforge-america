[build]
  publish = "dist"
  command = "npm run build"

# Edge Functions configuration
[[edge_functions]]
  function = "article-meta"
  path = "/article/*"

# SPA catch-all redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200