[build]
  publish = "dist"
  command = "npm run build"

# Using inline config in the Edge Function file instead

# SPA catch-all redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200